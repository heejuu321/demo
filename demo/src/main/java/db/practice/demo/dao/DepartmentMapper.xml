<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.dao.mapper.DepartmentMapper">
    <select id="selectAll" resultType="demo.dto.DepartmentDto">
        SELECT
            *
        FROM
            TB_DEPARTEMENT
    </select>
</mapper>